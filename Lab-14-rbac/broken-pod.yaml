apiVersion: v1
kind: Pod
metadata:
  name: api-explorer
spec:
  containers:
  - name: k8s-cli
    image: bitnami/kubectl:latest
    command: ["sh", "-c", "while true; do kubectl get pods; sleep 10; done"]