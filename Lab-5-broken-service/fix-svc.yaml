apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP
  selector:
    app: backend-api   ### Change tier to app since the pod label is app=backend-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80